import { Container, Typography } from "@mui/material";

export const CalcPage = () => {
  return (
    <Container maxWidth="xl">
      <Typography variant="h1">About what?</Typography>
    </Container>
  );
};
